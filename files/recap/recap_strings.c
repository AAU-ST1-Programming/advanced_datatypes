#include <stdio.h>
#include <strings.h>

int main()
{
    char a_string = "Martin"; // what is the problem here?

    printf("values in string variable (a_string) : %s\n", a_string);

    // copy string 1

    // copy string 2

    return 0;
}
