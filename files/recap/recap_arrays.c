#include <stdio.h>
#include <strings.h>

int main()
{
    int N = 10;

    // copy array1 (element by element)

    // copy array2 (memory copy, aaah so easy.. damn I hope this works!)
    return 0;
}
